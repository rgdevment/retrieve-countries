services:
  retrieve-countries:
    build: ./
    platform: linux/arm64
    container_name: retrieve-countries
    restart: unless-stopped
    env_file:
      - .env
    networks:
      - internal-net
    environment:
      - NODE_ENV=production
    expose:
      - 3000

networks:
  internal-net:
    external: true
